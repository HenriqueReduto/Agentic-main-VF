services:
  iseg-agent:
    build:
      context: .
      dockerfile: Dockerfile
    image: agentic-ai-iseg:latest
    container_name: iseg-agent
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - ./index:/app/index
    restart: unless-stopped